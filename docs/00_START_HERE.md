# Deep Research Seed Packet

This packet is the machine-oriented handoff bundle for the unified pool service operations platform build.

## Primary operating truth

The target system is a connector-first operating core where external systems flow through:

`raw → normalized → matched → approved → applied`

The strongest current code backbone is:

- `pool_service_platform_v3`

The bridge app is a real intake and communications workbench that should be absorbed into the unified platform as connector modules and front desk workflows, not kept as a permanent separate system.

The chemistry workbook is a deterministic model oracle and should be ported into code before later probabilistic updates.

## What this packet includes

- architecture truth files
- phased rollout guidance
- active baseline and stale-path manifest
- bridge-to-platform merge map
- canonical schema target notes
- curated code excerpt library
- full extracted copies of the uploaded code archives
- direct bridge source files
- bridge database schema export and table counts
- original archive manifests
- no live secrets or raw archives containing hidden secrets

## What to upload with this packet to Deep Research

Upload this packet plus these original files separately so Deep Research can inspect them directly:

1. `AI-Friendly Rollout Plan for a Connector-First Unified Pool Service Operations Core.pdf`
2. `Connector-First Unified Operating Core for a Pool Service Business.pdf`
3. `key_west_pool_consumption_coefficients_simple_updated_v2(1).xlsx`

Optional after redaction:
4. `bridge.db` only if you want the model to inspect real runtime state and sample records

Do not upload:
- live `.env`
- any unredacted credentials
- any unreviewed database with private contact, call, or message data

## Fast instruction for the next model

Treat this packet as the architecture anchor.
Use `pool_service_platform_v3` as the main code foundation.
Use the bridge as the intake and communications module seed.
Use the workbook as the deterministic chemistry oracle.
Mine `pool_service_pricing_v1` selectively for missing settings and normalized model concepts.
Mine `pool_volume_tool_live_proto` selectively for property intelligence and evidence-producing tooling.
Treat `RPC Root` as unrelated reference only.
